import React from "react";

const VotesBar = ({ votes }) => {
  return (
    <div id="votes-bar">
      <p>VotesBar Placeholder</p>
      <p>Votes: {votes} </p>
    </div>
  );
};

export default VotesBar;
